__pycache__/
*.pyc
*.pyo
.venv/
.env
.DS_Store
.ipynb_checkpoints/

# PNGs are committed so the README + GitHub Pages site can embed them.
# SVGs are vector backups; regeneratable via `make all` / `make all-en`, not tracked.
output/*.svg
output/en/*.svg

# Editors
.vscode/
.idea/
*.swp
